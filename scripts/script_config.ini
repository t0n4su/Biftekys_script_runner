[DEFAULT]
input_formats = .csv,.xlsx,.txt
output_format = excel
parameters = {
    "output_format": {
        "type": "dropdown",
        "label": "Output Format",
        "options": ["Excel", "CSV", "JSON"],
        "default": "Excel"
    },
    "remove_duplicates": {
        "type": "checkbox",
        "label": "Remove Duplicates",
        "default": true
    }
}